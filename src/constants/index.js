const WINDOW_WIDTH = window.innerWidth;
const WINDOW_HEIGHT = window.innerHeight;

// YouTube API constants
const PLAYER_WIDTH = WINDOW_WIDTH / 2 || 640;
const PLAYER_HEIGHT = 390;

// Component size
const SPINNER_RADIUS = {width: '4rem', height: '4rem', margin: `79px 0`};

const constants = {
  WINDOW_WIDTH,
  WINDOW_HEIGHT,
  PLAYER_WIDTH,
  PLAYER_HEIGHT,
  SPINNER_RADIUS
}

export default constants;