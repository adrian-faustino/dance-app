const WINDOW_WIDTH = window.innerWidth;
const WINDOW_HEIGHT = window.innerHeight;

// YouTube API constants
const PLAYER_WIDTH = WINDOW_WIDTH / 2 * 0.99 || 640;
const PLAYER_HEIGHT = 390;

const constants = {
  WINDOW_WIDTH,
  WINDOW_HEIGHT,
  PLAYER_WIDTH,
  PLAYER_HEIGHT,
}

export default constants;